# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# The whole site reads from Postgres, so this must be set or every page shows
# "Database not ready".
#
# Local dev: paste the connection string from your Neon dashboard
#   (Neon -> your project -> Connection Details -> copy the "psql"/URI string).
# Vercel: connecting a Postgres/Neon database under Storage adds POSTGRES_*
#   automatically, so you do not set this by hand there.
POSTGRES_URL=

# ---------------------------------------------------------------------------
# Admin session
# ---------------------------------------------------------------------------
# Signs the admin session cookie. Any long random string.
# Generate one with:  openssl rand -hex 32
#
# Administrator accounts are NOT configured here - they live in the
# admin_users table with scrypt-hashed passwords. On a fresh database, visit
# /admin/setup to create the first account; that page closes permanently once
# an account exists. Changing this secret signs everyone out.
ADMIN_SESSION_SECRET=
